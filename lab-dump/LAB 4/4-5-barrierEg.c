


/*

|| #pragma omp barrier || ->  

|| #pragma omp nowait || -> Allow thread to continue with work without waiting for other treads to finish their tasks


ULTRA HOT QUESTION -> Calculate the value of pi using Monte Carlo simulation 
	We don't know the value of pi. WHat we know is we have a circle, area is pi d2/4. Circle is placed inside a square, with area d2. 

Plot points in the circel and find if it is inside the circle or not. Find no of points that are inside the circle and assume the diameter from it.


*/
